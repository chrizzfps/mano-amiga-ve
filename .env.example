# ManoAmiga VE — variables de entorno
# La app corre en MODO MOCK (datos locales) si estas variables están vacías.
# Cuando crees tu proyecto en Supabase, copia este archivo a .env.local y rellena:

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Allowlist de correos con acceso al panel /admin (separados por coma).
VITE_ADMIN_EMAILS=
